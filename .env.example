# TempoTuner environment variables
#
# None required. The app is fully client-side (Web Audio API) with no backend
# services, databases, or scheduled jobs, so no environment variables need to
# be set locally or in the Vercel dashboard.
#
# If you add an env var, document it here and in DEPLOY.md.
